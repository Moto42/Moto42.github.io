<div class="hero_banner">
    <div class="hero_banner__left">
        <img class="hero_banner__img" src="{{img_src}}" alt="{{img_alt}}">
    </div>
    <div class="hero_banner__right">
        <h1>{{ h1_text }}</h1>
        <h2>{{h2_text}}</h2>
    </div>
</div>